import 'package:flutter/material.dart';

class HomeCampainContent extends StatelessWidget {
  const HomeCampainContent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
